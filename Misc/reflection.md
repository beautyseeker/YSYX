# 3/30
Nterm程序BUG排查:
1. `strtok`库函数调用理解含糊
2. `strdup`调用未意识到内存泄漏

nslider翻页BUG排查:
1. 已经`fs_open`打开过的图片文件对象再次打开它就会触发这个错误
2. libam中用NDL实现ioe.c过程错误，主要表现为ioe初始化，显存配置读取，键盘枚举值读取，显存写入均有错误，根本原因是滥用AI代码补全。

native环境运行am应用时fbctl初始化失败导致编译运行失败:
1. native的fsimg/路径缺少dev/fbctl文件
2. 

实现PA3的exec系统调用(Nanos环境)
上下文指针的值传递

1. 对NDL API与硬件抽象层的对应关系理解不到位。
2. 对C语言高阶宏用法不熟悉
3. 对C语言字符串库函数不熟悉
4. 上下文指针对象和指针本身的理解不到位
