# 1. 理解YEMU如何执行程序
- 画出在YEMU上执行的加法程序的状态机
- 通过RTFSC理解YEMU如何执行一条指令

思考一下, 以上两者有什么联系?
# 2. 理解NEMU指令执行过程
从cpu.pc中访存取出，为Decode结构体赋值，然后译码和执行全都在decode_exe()函数中执行，根据预先设定的字符模式匹配，通过各种辅助宏为指令码匹配部分设定对应的源、目标、立即数等操作数完成译码，根据模式匹配预定好的回写操作，完成一次执行。cpu->pc = s->dpc更新PC

# 3. 准备交叉编译环境

# 4. 运行第一个客户程序

# 5. 实现更多的指令
