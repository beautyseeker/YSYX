# 1. 为什么add函数和局部变量消失了，怎样才能算一个符号
因为add函数和它的局部变量仅在main函数局部调用，未被外部调用且函数功能仅仅是做个加法，编译器优化直接把它们在局部展开优化掉了，所以符号表找不到add函数符号定义。  
一个符号表至少要明确被其他地方引用或调用，且本身功能内容足够大不至于被编译器优化

# 2. 寻找Hello,World字符串
直接用`readelf -a exe`命令打开并搜索字符串找不到相关内容，原因是printf打印的内容过于简单，被编译器优化为puts处理了，且字符串作为常量存放在.rodata区而非此前的符号.symstab区了。